{
  "name": "@private-ai-agent/agent-protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared wire contract between the Agent runtime, gateways and client shells (event envelopes, chat turn schemas, unified errors).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
