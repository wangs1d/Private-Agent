# 多 Agent 协调器配置

# 启用多 Agent 协调模式（主 Agent 分发子 Agent）
ENABLE_MULTI_AGENT_COORDINATION=1

# 最大并行子任务数（默认 5）
MAX_PARALLEL_SUBTASKS=5

# 子任务超时时间（毫秒，默认 60000 = 60秒）
SUBTASK_TIMEOUT_MS=60000

# 主 Agent 使用的外部模型（需要在 external-model 配置中设置）
# EXTERNAL_MODEL_PROVIDER=kimi
# EXTERNAL_MODEL_API_KEY=your-api-key
