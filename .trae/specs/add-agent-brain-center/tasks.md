# Tasks

- [x] Task 1: 创建 `server/src/brain/` 目录骨架与核心类型
  - [x] SubTask 1.1: 新建 `server/src/brain/types.ts`，定义 `CapabilityDescriptor / UserActivityState / BrainDecision / EvolutionProposal / BrainSnapshot` 等类型
  - [x] SubTask 1.2: 新建 `server/src/brain/brain-center.ts`，实现 `BrainCenter` 外观类（持有四皮层引用 + `introspect/observe/decide/evolve/snapshot`）
  - [x] SubTask 1.3: 新建 `server/src/brain/index.ts` 对外导出
- [x] Task 2: 实现 `CapabilityCortex`（能力皮层）— 程序化能力自省，替代 prompt 注入
  - [x] SubTask 2.1: 新建 `server/src/brain/capability-cortex.ts`，把 `agent-capabilities.ts` 的 `CAPABILITY_DOMAINS / DOMAIN_LABELS` 迁为运行时可注册的 `CapabilityRegistry`
  - [x] SubTask 2.2: 实现 `list() / has(domain) / identifyGap(scenario) / expand(proposal)` 方法（规则驱动，无 LLM）
  - [x] SubTask 2.3: 改造 `server/src/agent/agent-capabilities.ts`：`buildAgentCapabilityPromptSection` 从 `CapabilityCortex.snapshot()` 取数据；system prompt 只保留最简摘要 + 指引调用 `brain.list_capabilities`
- [x] Task 3: 实现 `AwarenessCortex`（感知皮层）— 统一 UserActivityState
  - [x] SubTask 3.1: 新建 `server/src/brain/awareness-cortex.ts`，注册 `LifeSignalHubService / DesktopPresenceSignalService / MoodInferenceService / AnticipationEngineService` 为子系统（不重写）
  - [x] SubTask 3.2: 实现 `UserActivityState` 推断规则：`just_off_work / going_out / idle / busy / sleeping`（基于 desktop presence + 时间节律 + life signal 关键词匹配）
  - [x] SubTask 3.3: 暴露 `observe(actorId)` 查询接口 + 事件订阅 `onActivityChange`
- [x] Task 4: 实现 `ProactionCortex`（主动皮层）— 单点决策流水线
  - [x] SubTask 4.1: 新建 `server/src/brain/proaction-cortex.ts`，以 `JarvisDecisionEngine` 为唯一决策入口
  - [x] SubTask 4.2: 整合 `ProactiveAgentCenter / ProactiveLifeRuntimeService` 的判定逻辑（contact policy / cooldown / 时间节律闸门）
  - [x] SubTask 4.3: 在 `create-app-services.ts` 默认不再独立 `start()` 两个旧服务，由 ProactionCortex 统一驱动
  - [x] SubTask 4.4: 支持 `BRAIN_PROACTION_LEGACY=1` 降级回旧路径（ProactionCortex 进 shadow 模式）
- [x] Task 5: 实现 `EvolutionCortex`（进化皮层）— 编排自我进化
  - [x] SubTask 5.1: 新建 `server/src/brain/evolution-cortex.ts`，注册 `AgentSelfLearningService / SkillGenerator / SkillPromotionPipeline / HermesEvolutionLoopService` 为子系统
  - [x] SubTask 5.2: 实现 `proposeCapability / evolve(proposalId) / gapReport()` 接口
  - [x] SubTask 5.3: 实现 `EvolutionProposal` 状态机：`pending → reviewing → approved/rejected → generated/loaded`
- [x] Task 6: 在 `create-app-services.ts` 装配 BrainCenter
  - [x] SubTask 6.1: 实例化四个皮层，注册现有子系统（不重写子系统的内部实现）
  - [x] SubTask 6.2: 把 `brainCenter` 注入 `agentCore` / `toolRegistry` / HTTP 路由依赖
  - [x] SubTask 6.3: 支持 `BRAIN_CENTER_ENABLED=0` 完全降级（不实例化 brain/，现状不受影响）
- [x] Task 7: 暴露 HTTP 路由 `server/src/routes/http/brain.ts`
  - [x] SubTask 7.1: `GET /brain/capabilities` — 程序化能力清单
  - [x] SubTask 7.2: `GET /brain/state` — 感知 + 决策状态快照
  - [x] SubTask 7.3: `POST /brain/proactive/test` — 注入测试信号走完整决策流水线
  - [x] SubTask 7.4: `POST /brain/evolve/propose` — 提交能力缺口提案
  - [x] SubTask 7.5: 在 `routes/http/index.ts` 注册
- [x] Task 8: 注册 agent 工具 `server/src/tools/brain-tools.ts`（让 agent 自己调用，非 prompt）
  - [x] SubTask 8.1: `brain.list_capabilities` — 查询能力域
  - [x] SubTask 8.2: `brain.identify_gap` — 描述场景返回缺失能力
  - [x] SubTask 8.3: `brain.propose_capability` — 提议新增能力
  - [x] SubTask 8.4: `brain.observe_user` — 查询当前用户活动状态
  - [x] SubTask 8.5: 在 `tool-registry.ts` / `getBuiltinAgentChatTools()` 注册
- [x] Task 9: 验证与回归
  - [x] SubTask 9.1: `tsc --noEmit` 通过零错误
  - [x] SubTask 9.2: 启动 server，确认 `BrainCenter.start()` 后四个皮层均正常 register
  - [x] SubTask 9.3: 验证旧 proactive / life-signal 流在 `BRAIN_CENTER_ENABLED=0` 下降级仍工作
  - [x] SubTask 9.4: 调用 `GET /brain/capabilities` 与 `brain.list_capabilities` 工具返回一致
  - [x] SubTask 9.5: 注入"刚下班"测试信号，验证 ProactionCortex 能产出决策（含 value/disturb 评分与 rationale）

# Task Dependencies

- Task 2 / 3 / 4 / 5 依赖 Task 1（类型 + BrainCenter 骨架）
- Task 2 / 3 / 4 / 5 之间相互独立，可并行
- Task 6 依赖 Task 2 / 3 / 4 / 5（四个皮层实现完成）
- Task 7 / 8 依赖 Task 6（BrainCenter 已装配）
- Task 9 依赖 Task 7 / 8
