- [x] `server/src/brain/types.ts` 已扩展 `SensoryFrame / MemoryItem / SynapseMessage / SafetyCheckResult / EmotionVector / PlanStep / PlanResult / SystemRouteDecision` 等类型
- [x] `server/src/brain/sensory-cortex.ts` 已创建，注册 `VoiceDialogueService / VoiceCapabilityService / 桌面视觉子系统`，暴露 `listen / look / speak / endToEndVoice`
- [x] `server/src/brain/memory-cortex.ts` 已创建，注册 `ShortTermMemoryGateway / AgenticMemoryRuntime / HumanLikeMemoryService / NarrativeMemoryFacade`，暴露 `remember / recall / recallCrossDomain / consolidate`
- [x] `server/src/brain/synapse-bus.ts` 已创建，以 `HookBus` 为底座，桥接 `MessageHubService / AipService / WsConnectionRegistry`，暴露 `fire / subscribe / sendToAgent / sendToUser`
- [x] `server/src/brain/limbic-cortex.ts` 已创建，注册 `AgentTaskSafety / MoodInferenceService / AssistantTonePolicy / EmotionTone`，暴露 `checkSafety / inferEmotion / applyTonePolicy`
- [x] `server/src/brain/planner-cortex.ts` 已创建，注册 `PlanExecuteLoop / MasterAgentCoordinator / TaskRouter`，暴露 `plan / execute / react / delegate / routeSystem`
- [x] `BrainCenter` 已扩展为持有 9 分区（4 旧 + 5 新），新增 5 个 `registerXxx` 方法 + 9 个核心方法（`listen / look / speak / remember / recall / fire / checkSafety / plan / routeSystem`）
- [x] 旧 4 皮层方法（`introspect / observe / decide / evolve / snapshot`）签名与行为保持向后兼容
- [x] `snapshot(actorId)` 返回包含 9 个分区状态的 `BrainSnapshot`（新字段可选）
- [x] `server/src/brain/index.ts` 已导出 5 个新分区类
- [x] `create-app-services.ts` 已实例化 5 个新分区并注册现有感官/记忆/通信/安全/规划服务为子系统（未重写子系统实现）
- [x] `BRAIN_NEURO_ENABLED=0` 可降级，5 个新分区不实例化，BrainCenter 回退到 4 皮层模式
- [x] `HookEventType` 枚举已扩展 `sensory.* / memory.* / synapse.* / limbic.* / planner.*` 类型
- [x] HTTP 路由 `/brain/sensory/* / /brain/memory/* / /brain/synapse/* / /brain/limbic/* / /brain/planner/*` 已注册并可用
- [x] agent 工具 `brain.listen / brain.look / brain.speak / brain.remember / brain.recall / brain.check_safety / brain.plan / brain.route_system` 已注册到 `BRAIN_TOOLS`
- [x] `tsc --noEmit` 通过零错误
- [x] 现有 4 皮层在 `BRAIN_NEURO_ENABLED=0` 降级模式下行为不变
- [x] 全程未引入"用 prompt 让 LLM 自我反思感官/记忆/通信/安全/规划"的实现路径
- [x] 危险操作（如 `rm -rf`）经 `limbic.checkSafety` 必返回 DENIED
- [x] 快慢双系统路由（`planner.routeSystem`）能正确返回 `system1` 或 `system2` + 对应 mode
