{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认能力 - 自定义 IPC 命令通过 core:default 即可调用",
  "windows": ["main", "schedule"],
  "permissions": [
    "core:default"
  ]
}
